version: '3.1'

services:
  db: 
    image: postgres
    restart: always
    ports:
      - "8295:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: keyline